package com.codeapes.checklist.web.util.viewhelper;

public abstract class AbstractViewHelper<T> implements ViewHelper<T> {

    public AbstractViewHelper(T inputObject) {

        //Check for mapping info in cache
        //If found, map it
        //If not found, load the cache.
    }

}
